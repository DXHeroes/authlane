## Summary

<!-- What does this change and why? Link the issue if one exists. -->

## Checklist

- [ ] `pnpm lint`, `pnpm type-check`, and `pnpm test` pass locally
- [ ] `pnpm docs:check` passes (if documentation was touched, ran `pnpm docs:generate`)
- [ ] Added a changeset (`pnpm changeset`) if a publishable package changed
