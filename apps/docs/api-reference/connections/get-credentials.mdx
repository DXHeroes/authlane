---
title: 'Get access credentials'
api: 'GET /api/v1/users/{externalUserId}/connections/{serviceId}/credentials'
---

Returns access-only credential material for direct provider calls. Requires `credentials:read`. The response is audited, uses `Cache-Control: no-store`, and excludes OAuth refresh tokens.
